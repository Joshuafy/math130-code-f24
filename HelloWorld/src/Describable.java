public interface Describable {
    // Any class that implements the Describable interface
    // will need to implement the describeClass method
    // with the following signature
    String describeClass();
}
